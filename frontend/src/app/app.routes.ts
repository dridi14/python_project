import { Routes } from '@angular/router';
import { AppComponent } from './app.component';
import { ForceGraphComponent } from './force-graph/force-graph.component';

export const routes: Routes = [
  { path: '', component: ForceGraphComponent },

];
